const headerMessage = document.querySelector('.header-message')
if (headerMessage && headerMessage instanceof HTMLElement && headerMessage.innerText === 'Welcome to Scaleflex Code') {
  headerMessage.style.background = '#002000'
  headerMessage.style.color = '#fff'
}

